# Environment Configuration
# ------------------

# Environment (development, testing, production)
ENVIRONMENT=development

# Debug mode (True/False)
DEBUG=True

# API Settings
# ------------------
API_V1_STR=/api/v1

# CORS Settings
# ------------------
# Comma-separated list of allowed origins
# Development: CORS_ORIGINS=http://localhost,http://localhost:8000,http://localhost:3000
# Testing: CORS_ORIGINS=*
# Production: CORS_ORIGINS=https://yourdomain.com
CORS_ORIGINS=http://localhost,http://localhost:8000,http://localhost:3000

# Service Information
# ------------------
SERVICE_NAME=In-Memory Database Service
SERVICE_VERSION=1.0.0
SERVICE_DESCRIPTION=A FastAPI service providing in-memory database operations 